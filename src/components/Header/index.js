import React from 'react';
import Logo from '../Logo/index';

const Header = () => {

    return (
        <header className="Header">
            <Logo />
        </header>
    )

};

export default Header;